/** Auto-theme constants. */

/** Theme name to use when the terminal is in dark mode. */
export const THEME_DARK = "dark";

/** Theme name to use when the terminal is in light mode. */
export const THEME_LIGHT = "light";

/** How long to wait for a Mode 2031 query response before giving up. */
export const QUERY_TIMEOUT_MS = 1500;
