/**
 * Comment injected into managed config files to point readers back to the
 * harness, which is the source of truth for these settings.
 */
export const SOURCE_OF_TRUTH_COMMENT =
  "Managed by pi-harness (hooks/default-settings). Source of truth: pi-harness/hooks/default-settings.";
